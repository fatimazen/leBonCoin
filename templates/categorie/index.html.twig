{% extends 'base.html.twig' %}

{% block body %}

	<div class="form-group">
		<nav class="navbar navbar-light bg-light">

			<form class="form-inline">
				<input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
				<button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
			</form>
		</nav>

		<label for="selection">Trie: plus récentes</label>
		<select id="selection" class="form-control">
			<option value="">tri:plus récentes</option>
			<option value="">tri:plus ancienne</option>
			<option value="">tri:croissants</option>
			<option value="">tri:dècroissants</option>
		</select>
	</div>
	{% for annonce in annonces %}

		<div class="card mb-3" style="width: 18rem; border:1px solid #000 ;border-radius:8px; margin-top:150px">

			<div class="row  gutters">
				<div class="row-md-4">
					<img src="https://picsum.photos/200/300" class="card-img-top" style="width:100%; height:250px" alt="...">

					<div class="card-body" style:"row-md-4">

						<h5 class="card-title " style="text-align:center; font-size:18px"></h5>
						<div class="description">
							<p class="card-text" style="padding:0 10px">{{annonce.title}}</p>
						</div>
						<div>
						<p>{{annonce.city}} {{annonce.zipCity}}</p>
							</div>
							<div>
							<p>{{annonce.CreationDate ? annonce.CreationDate|date('d/m/y') : ''}}</p>
							</div>
						</div>
					</div>
				{% endfor %}
			</div>
			<a href="#" class="btn btn-primary">Détails</a>

		{% endblock %}
